// EW-266 — duplicate of `src/components/HomepageFeatures/help.tsx` (see the note in
// `./index.tsx`). Re-exported rather than copied so the two paths cannot drift.
export { default } from '../HomepageFeatures/help';
